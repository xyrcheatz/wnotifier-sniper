{
  "name": "wnotifier-cloudflare-complete",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply wnotifier-cloud-db --local",
    "db:migrate": "wrangler d1 migrations apply wnotifier-cloud-db --remote"
  },
  "devDependencies": {
    "@cloudflare/playwright": "^1.3.0",
    "wrangler": "^4.0.0"
  }
}
